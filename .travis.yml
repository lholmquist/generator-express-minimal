language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-express-minimal' ]; then cd .. && eval "mv $currentfolder generator-express-minimal" && cd generator-express-minimal; fi

