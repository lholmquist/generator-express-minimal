language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-express-simple' ]; then cd .. && eval "mv $currentfolder generator-express-simple" && cd generator-express-simple; fi

